# SLICE 2026 Symposium Website Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a premium, responsive, single-page website for the SLICE 2026 college symposium using separate static files, ready for free Vercel deployment.

**Architecture:** A three-file static site structure (`index.html`, `style.css`, and `app.js`) with an `assets` folder containing the ECE department's under-construction image. Dynamic data (events) is managed in JS to allow easy text edits.

**Tech Stack:** HTML5, Vanilla CSS3 (Custom Properties / Design Tokens), Vanilla JavaScript (ES6+).

## Global Constraints
- **File Structure:** All code must be in `index.html`, `style.css`, and `app.js`. No npm package bundlers or dependencies.
- **Visual Design:** Dark theme (`#0B0F19`), glassmorphism cards (`rgba(20, 26, 45, 0.6)` + background blur), neon cyan (`#00F2FE`) and purple (`#4FACFE`) accents.
- **Assets:** The ECE department under-construction banner must be displayed in the hero/about area.

---

### Task 1: Document Structure & HTML Scaffolding

**Files:**
- Create: `index.html`

**Interfaces:**
- Produces: Complete HTML structure linking to Google Fonts (Outfit, Inter), `style.css`, and `app.js`.

- [ ] **Step 1: Create the HTML5 scaffold**
  Write the core HTML structure inside `index.html` with correct meta tags, Google Font preconnects, and scripts:
  ```html
  <!DOCTYPE html>
  <html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>SLICE 2026 | ECE Department Symposium</title>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Outfit:wght@500;700;800&display=swap" rel="stylesheet">
      <link rel="stylesheet" href="style.css">
  </head>
  <body>
      <!-- Structure: Header, Hero, About, Events, Schedule, FAQs, Contact/Footer -->
      <script src="app.js" defer></script>
  </body>
  </html>
  ```

- [ ] **Step 2: Add navigation and core section wrappers**
  Add semantic section tags inside `<body>`:
  - `<header>` with class `navbar` containing the nav links.
  - `<main>` containing:
    - `<section id="home" class="hero">`
    - `<section id="about" class="about">`
    - `<section id="events" class="events">`
    - `<section id="schedule" class="schedule">`
    - `<section id="faqs" class="faqs">`
    - `<section id="contact" class="contact">`
  - `<footer>`

- [ ] **Step 3: Scaffold the DOM nodes for interactive content**
  - Inside the Hero section: Add containers for the Title, Subtitle, Countdown Timer (`#days`, `#hours`, `#minutes`, `#seconds`), and a "Register Now" anchor button.
  - Inside the About section: Add an image tag referencing `assets/ece_under_construction.jpg`.
  - Inside the Events section: Add filter buttons (`.filter-btn`) and an empty grid wrapper `#events-grid` for dynamic rendering. Add the modal layout (`#event-modal`) at the bottom of the body.
  - Inside the FAQ section: Add questions and answer panels.

- [ ] **Step 4: Manual Verification**
  Open the raw `index.html` file in a browser. 
  Expected result: A plain white page displaying unstyled text, links, and the ECE under-construction image.

---

### Task 2: CSS Design System and Premium Aesthetics

**Files:**
- Create: `style.css`

**Interfaces:**
- Consumes: HTML structure and assets from Task 1.

- [ ] **Step 1: Define CSS Variables and CSS Reset**
  Establish the color palette, fonts, and box sizing inside `style.css`:
  ```css
  :root {
      --bg-color: #0B0F19;
      --card-bg: rgba(20, 26, 45, 0.65);
      --card-border: rgba(0, 242, 254, 0.15);
      --primary: #00F2FE;
      --secondary: #4FACFE;
      --text-main: #FFFFFF;
      --text-muted: #94A3B8;
      --font-heading: 'Outfit', sans-serif;
      --font-body: 'Inter', sans-serif;
  }
  * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      scroll-behavior: smooth;
  }
  body {
      background-color: var(--bg-color);
      color: var(--text-main);
      font-family: var(--font-body);
      overflow-x: hidden;
  }
  ```

- [ ] **Step 2: Style Navigation and Hero Section**
  - Implement a sticky navbar using `position: sticky; top: 0; backdrop-filter: blur(10px);`.
  - Style the Hero section using a full-height centered Flexbox. Create a glowing text effect for `SLICE 2026` and a modern countdown layout displaying numbers in glassmorphism circular boxes.
  - Style buttons with a glowing hover effect (`box-shadow: 0 0 15px var(--primary)`).

- [ ] **Step 3: Style Glassmorphism Cards, Grid, and Modal**
  - Apply the glass card rules to `.event-card`: `background: var(--card-bg); backdrop-filter: blur(8px); border: 1px solid var(--card-border); border-radius: 12px; transition: transform 0.3s ease, border-color 0.3s ease`.
  - Add hover transitions that lift the cards and glow the border cyan.
  - Design the popup modal style: hidden by default (`display: none; opacity: 0;`), centers on screen using fixed position, with a dark background overlay (`rgba(0, 0, 0, 0.8)`).

- [ ] **Step 4: Style Schedule, FAQ accordion, and Footer**
  - Create vertical line/circles timeline styles.
  - Create accordion transitions (adjusting height and rotation of chevron icons).
  - Clean up footer alignment and social icons styling.

- [ ] **Step 5: Manual Verification**
  Open the file in the browser. 
  Expected result: Dark mode theme is active, sticky header blurs content underneath, hero has glowing buttons, cards are styled correctly in a grid, and page is responsive on mobile viewport widths.

---

### Task 3: Interactive Features & JavaScript Logic

**Files:**
- Create: `app.js`

**Interfaces:**
- Consumes: DOM nodes scaffolded in Task 1.

- [ ] **Step 1: Implement the Countdown Timer**
  Write the countdown logic targeting the symposium date (e.g. October 15, 2026):
  ```javascript
  const targetDate = new Date("Oct 15, 2026 09:00:00").getTime();

  function updateCountdown() {
      const now = new Date().getTime();
      const difference = targetDate - now;

      const days = Math.floor(difference / (1000 * 60 * 60 * 24));
      const hours = Math.floor((difference % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((difference % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((difference % (1000 * 60)) / 1000);

      document.getElementById("days").innerText = days.toString().padStart(2, '0');
      document.getElementById("hours").innerText = hours.toString().padStart(2, '0');
      document.getElementById("minutes").innerText = minutes.toString().padStart(2, '0');
      document.getElementById("seconds").innerText = seconds.toString().padStart(2, '0');
  }
  setInterval(updateCountdown, 1000);
  updateCountdown();
  ```

- [ ] **Step 2: Add Events Data Array and Dynamic Grid Rendering**
  Create a data structure in `app.js` holding the event information:
  ```javascript
  const events = [
      {
          id: "coding",
          title: "Coding Quest",
          category: "technical",
          prize: "₹5000",
          desc: "Test your speed and debugging skills in a series of progressive coding challenges.",
          rules: ["Individual participation.", "Time limit: 45 minutes.", "Languages allowed: C++, Java, Python."],
          contacts: "Coordinator A: 9876543210"
      },
      // ... Add WebCraft, Paper Pitch, Cyber Gaming, LensCraft, Mystery Hunt, Workshops
  ];
  ```
  Write a function to generate HTML cards and append them to `#events-grid`.

- [ ] **Step 3: Implement Filters and Modals**
  - Add event listeners to filter buttons. Filter the dynamic cards in the grid by toggling their display style depending on their category attribute.
  - Implement modal open/close functions. Set content of `#event-modal` dynamically from the event array based on which card clicked, and show/hide the modal with opacity transition.
  - Add close button and close-on-outside-click handlers.

- [ ] **Step 4: Implement FAQ Accordion toggle**
  - Select all FAQ header elements. Add event listeners to toggle `.active` classes and control visual height of the answers.

- [ ] **Step 5: Manual Verification**
  Reload the page:
  - Check that the countdown changes second-by-second.
  - Click on "Technical" filter; verify that non-technical cards disappear.
  - Click "View Details" on a card; verify that a modal pops up with correct rules and contacts. Click close; verify it closes.
  - Click FAQ headings; verify they expand and collapse.

---

### Task 4: Local Server Setup and Final Review

**Files:**
- Modify: `index.html` (verify paths)

- [ ] **Step 1: Check assets and layout boundaries**
  Ensure path to the image `assets/ece_under_construction.jpg` resolves perfectly.
  
- [ ] **Step 2: Run local web server and verify compatibility**
  Verify the layout on modern browsers. Check browser developer console to ensure there are no 404 image errors, mixed-content warnings, or JavaScript syntax errors.

---

### Task 5: Git Version Control and Vercel Deployment

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: Create a basic .gitignore file**
  Add files that shouldn't be pushed (e.g. system files or temp files):
  ```gitignore
  .DS_Store
  Thumbs.db
  .agents/
  .gemini/
  ```

- [ ] **Step 2: Initialize Git and Commit**
  Run:
  ```bash
  git init
  git add .
  git commit -m "feat: initial release of SLICE 2026 symposium website"
  ```
